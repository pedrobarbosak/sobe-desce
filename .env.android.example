# Copy to .env.android. The deployment the Android app talks to, compiled into the app
# by scripts/android.sh; the same three values the site is built with (see .env.deploy).
VITE_CONVEX_URL=https://sobe-desce-api.example.com
VITE_CONVEX_SITE_URL=https://sobe-desce-actions.example.com
VITE_APP_URL=https://sobe-desce.example.com
